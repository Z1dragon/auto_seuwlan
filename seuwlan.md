# SEU-WLAN Portal link

Paste your redirected SEU-WLAN login URL here if automatic portal detection does not work.

For privacy, do not commit a real URL containing your assigned `UserIP`, AP name, account, or password.

Example shape only, without a usable URL:

```text
w.seu.edu.cn/a79.htm?UserIP=10.x.x.x&wlanacname=...
```
